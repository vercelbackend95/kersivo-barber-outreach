---
import MainLayout from '@/layouts/MainLayout.astro';
import LegalFooter from '@/components/LegalFooter.astro';
import { LEGAL_OPERATOR_NAME, TRADING_NAME } from '@/lib/legal/businessIdentity';
import { PAGE_DESCRIPTIONS } from '@/lib/seo/defaults';
import { CONSENT_COOKIE_NAME, CONSENT_MAX_AGE_SECONDS, CONSENT_VERSION } from '@/lib/consent/config';
import '@/styles/legal-pages.css';

const consentDays = Math.round(CONSENT_MAX_AGE_SECONDS / (24 * 60 * 60));
---

<MainLayout
  title="Cookie Policy | Kersivo"
  description={PAGE_DESCRIPTIONS.cookies}
  canonicalPath="/cookies"
>
  <main class="legal-page">
    <article class="container legal-page__inner">
      <h1 class="legal-page__title">Cookie Policy</h1>
      <p class="legal-page__updated">Last updated: 25 September 2026</p>

      <p>
        This Cookie Policy explains how KERSIVO uses cookies and similar technologies on
        <a href="https://kersivo.co.uk/">kersivo.co.uk</a>. It should be read with our
        <a href="/privacy">Privacy Policy</a>. Rejecting optional technologies does not block necessary services such as
        browsing, booking demos, contact forms or paying for a monthly subscription. Mentions of a setup deposit below
        refer to a gated legacy checkout path that is not currently offered publicly.
      </p>

      <h2>What are cookies and similar technologies?</h2>
      <p>
        Cookies are small text files stored on your device. Similar technologies include localStorage, sessionStorage
        and tags/scripts that set identifiers. Some are necessary for the site to work. Others help us understand site
        performance and are used only with your permission.
      </p>

      <h2>How we ask for consent</h2>
      <p>
        On your first visit (and again when our consent version changes), we show a banner with comparable actions to
        accept all optional technologies, reject optional technologies, or manage preferences. Your choice is stored in
        a first-party necessary cookie (<code>{CONSENT_COOKIE_NAME}</code>, consent version {CONSENT_VERSION}, about{' '}
        {consentDays} days). You can reopen preferences at any time via <strong>Cookie settings</strong> in the footer.
      </p>
      <p>
        We use Google Consent Mode v2 with a privacy-first approach: analytics tags are not loaded until you grant
        Analytics permission. Advertising-related Consent Mode signals remain denied while Google Ads is inactive on
        Production (see below). The marketing-site cookie consent controls described in this policy govern
        {TRADING_NAME} marketing surfaces. They do not govern live tenant barbershop shop journeys. {TRADING_NAME}
        marketing analytics tags are not loaded on live barbershop booking or storefront pages. That does not mean every
        third-party operational processor used to run the service (for example hosting, payments, email or monitoring) is
        absent from those pages.
      </p>

      <h2>Categories we use</h2>
      <h3>Necessary</h3>
      <p>
        Required for security, payments, forms, booking functionality, remembering your privacy choices, and keeping a
        barbershop admin signed in so you can manage your workspace securely without logging in on every visit. When a
        valid admin session cookie is present, opening the Admin &amp; Shop product preview takes you straight to your
        signed-in workspace (without signing in with Google again). These cannot be switched off through the preference
        tool.
      </p>
      <h3>Analytics</h3>
      <p>
        Off by default. When accepted, Google Analytics 4 helps us understand how visitors use the site. Provider:
        Google. This is currently the only optional category offered in Cookie settings on Production.
      </p>
      <h3>Google Ads (currently inactive)</h3>
      <p>
        Google Ads measurement and personalised advertising / remarketing are <strong>not currently configured</strong>
        on Production. Cookie settings do not offer Advertising measurement or Personalised advertising choices while
        Ads remains dormant. Historical Google-held Ads records (if any) are not claimed erased from Google&rsquo;s
        systems merely because Ads is inactive on this website.
      </p>

      <h2>Technology inventory</h2>
      <p>
        The table below lists technologies we have verified in the KERSIVO application. Google cookie durations can vary
        by Google product configuration; where we cannot verify an exact TTL from our code alone, duration is marked as
        unresolved pending browser verification after consent.
      </p>

      <div class="legal-page__table-wrap">
        <table class="legal-page__table">
          <thead>
            <tr>
              <th scope="col">Name</th>
              <th scope="col">Provider</th>
              <th scope="col">Category</th>
              <th scope="col">Purpose</th>
              <th scope="col">Duration</th>
              <th scope="col">Party</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td><code>{CONSENT_COOKIE_NAME}</code></td>
              <td>KERSIVO</td>
              <td>Necessary</td>
              <td>Stores your cookie preference version and choices</td>
              <td>{consentDays} days</td>
              <td>First-party cookie</td>
            </tr>
            <tr>
              <td><code>kersivo_admin_session</code></td>
              <td>KERSIVO</td>
              <td>Necessary</td>
              <td>Keeps an authenticated barbershop admin signed in to the admin panel</td>
              <td>30 days</td>
              <td>First-party cookie</td>
            </tr>
            <tr>
              <td>
                <code>better-auth.session_token</code> (on HTTPS:
                <code>__Secure-better-auth.session_token</code>)
              </td>
              <td>KERSIVO</td>
              <td>Necessary</td>
              <td>Primary admin authentication session; keeps you signed in to the admin panel and opens your workspace from the Admin &amp; Shop preview when the cookie is valid</td>
              <td>Up to 30 days</td>
              <td>First-party cookie</td>
            </tr>
            <tr>
              <td><code>kersivo_shop_cart_v2</code></td>
              <td>KERSIVO</td>
              <td>Necessary</td>
              <td>Retail demo basket contents. The BLACKLINE demo uses <code>kersivo_shop_cart_v2:blackline-barbers-demo</code> so bags stay shop-scoped.</td>
              <td>Until cleared / localStorage</td>
              <td>First-party localStorage</td>
            </tr>
            <tr>
              <td><code>kersivo.admin.secret</code></td>
              <td>KERSIVO</td>
              <td>Necessary</td>
              <td>Admin demo authentication helper</td>
              <td>Until cleared / localStorage</td>
              <td>First-party localStorage</td>
            </tr>
            <tr>
              <td><code>feature261-reports-widget-v1</code></td>
              <td>KERSIVO</td>
              <td>Necessary</td>
              <td>Landing demo reports widget display preferences</td>
              <td>Session</td>
              <td>First-party sessionStorage</td>
            </tr>
            <tr>
              <td><code>setup_deposit_paid:*</code></td>
              <td>KERSIVO</td>
              <td>Analytics (when consented)</td>
              <td>Prevents duplicate setup deposit conversion events on refresh</td>
              <td>Session</td>
              <td>First-party sessionStorage</td>
            </tr>
            <tr>
              <td><code>_ga</code>, <code>_ga_*</code></td>
              <td>Google Analytics</td>
              <td>Analytics (when consented)</td>
              <td>Distinguish visitors / sessions for GA4</td>
              <td>Up to 2 years by provider default, subject to browser limits</td>
              <td>First-party cookies set via Google</td>
            </tr>
            <tr>
              <td><code>_gcl_*</code> / <code>_gac*</code> / <code>_gcl_ls</code> (historical / dormant)</td>
              <td>Google Ads</td>
              <td>Inactive — not currently set by Production</td>
              <td>
                Previously associated with Google Ads measurement / remarketing when Ads was configured. Production
                currently has no Google Ads ID and does not create these identifiers. Any residual first-party keys from
                older visits are cleared automatically when consent is invalid or Ads capability is absent.
              </td>
              <td>N/A while Ads dormant</td>
              <td>First-party (historical)</td>
            </tr>
            <tr>
              <td>URL parameters (<code>gclid</code>, <code>gbraid</code>, <code>wbraid</code>, UTMs)</td>
              <td>Page URL / GA4 (when Analytics consent is active)</td>
              <td>Analytics consent (GA4 only)</td>
              <td>
                May remain in the page URL. GA4 may process relevant campaign/source information only when Analytics
                consent is active. KERSIVO does not pass these identifiers into setup/subscription checkout, Stripe
                metadata, or internal fulfilment email, and does not store them in a dedicated attribution cookie or
                localStorage. Google Ads remains inactive / dormant on Production.
              </td>
              <td>URL lifetime; GA4 per Analytics settings</td>
              <td>Not a browser cookie</td>
            </tr>
          </tbody>
        </table>
      </div>

      <h2>Changing or withdrawing consent</h2>
      <p>
        Use <strong>Cookie settings</strong> in the footer. If you withdraw Analytics, we update Google Consent Mode
        immediately, stop loading the GA4 tag on later visits, and delete removable first-party Analytics cookies and
        related sessionStorage keys where technically possible. Cookies set on other domains by third parties cannot
        always be deleted from our site; those tags will not be loaded again after withdrawal.
      </p>

      <h2>Contact</h2>
      <p>
        {TRADING_NAME} is a trading name of {LEGAL_OPERATOR_NAME}. Questions:
        <a href="mailto:hello@kersivo.co.uk">hello@kersivo.co.uk</a>. Complaints may be made to the
        <a href="https://ico.org.uk/">ICO</a>.
      </p>
    </article>
  </main>
  <LegalFooter />
</MainLayout>
