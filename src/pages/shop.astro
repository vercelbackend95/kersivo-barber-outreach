---
import MainLayout from '@/layouts/MainLayout.astro';
import { prisma } from '../lib/db/client';
import { resolveShopId } from '../lib/db/shopScope';
import { formatGbp } from '../lib/shop/money';


const shopId = await resolveShopId();
const products = await prisma.product.findMany({
  where: { shopId, active: true },
  orderBy: [{ sortOrder: 'asc' }, { featured: 'desc' }, { updatedAt: 'desc' }]
});

const CATEGORY_LABELS: Record<string, string> = {
  POMADES_AND_CLAYS: 'Pomades',
  BEARD_CARE: 'Beard',
  HAIR_WASH: 'Wash',
  STYLING: 'Styling',
  TOOLS: 'Tools',
  GIFT_SETS: 'Sets'
};
const categoryFilters = [
  { value: 'ALL', label: 'All products', count: products.length },
  ...Object.entries(CATEGORY_LABELS).map(([value, label]) => ({
    value,
    label,
    count: products.filter((product) => product.category === value).length
  }))
].filter((filter, index, allFilters) => filter.value === 'ALL' || allFilters[index]?.count > 0);

const featuredPromotion = {
  kicker: 'Elevate your routine',
  title: 'Premium grooming products selected by barbers.',
  ctaLabel: 'Shop all products',
  ctaLink: '/shop'
};


---

<MainLayout title="Shop | Kersivo Barber Outreach">
    <main class="container shop-page" aria-labelledby="shop-page-title">
    <header class="shop-header-block">
      <p class="shop-eyebrow">Curated grooming store</p>
      <h1 id="shop-page-title">Barber retail essentials</h1>
      <p class="shop-intro">A clean product layout inspired by editorial storefronts. Browse quickly, add to cart in one click, and collect in the shop.</p>
            <div class="shop-category-filters" role="group" aria-label="Filter by category">
        {categoryFilters.map((filter) => (
          <button
            type="button"
            class:list={["shop-category-filter", filter.value === 'ALL' && 'is-active']}
            data-category-filter={filter.value}
            aria-pressed={filter.value === 'ALL' ? 'true' : 'false'}
          >
            <span>{filter.label}</span>
            <span class="shop-category-filter__count" aria-hidden="true">{filter.count}</span>
          </button>
        ))}
      </div>
    </header>
    <section class="shop-layout" aria-label="Products">
      {products.length === 0 ? (
        <article class="shop-promo-card shop-promo-card--empty">

          <p class="shop-promo-kicker">Retail built in</p>
          <h2>Add products to launch your storefront</h2>
          <p>Publish products in admin and they will appear here instantly.</p>
          <a class="btn btn--primary" href="/shop">Open shop page</a>

        </article>
      ) : (
        <ul class="shop-grid" aria-label="Products">
          <li class="shop-grid-promo-slot">

            <article class="shop-promo-card">
              <p class="shop-promo-kicker">{featuredPromotion.kicker}</p>
              <h2>{featuredPromotion.title}</h2>
              <a class="btn btn--primary" href={featuredPromotion.ctaLink}>{featuredPromotion.ctaLabel}</a>

            </article>
          </li>

          {products.map((product) => (
            <li data-product-item data-product-category={product.category}>
              <article class="shop-card" data-category={product.category}>
                <a href={`/shop/${product.id}`} class="shop-media-link" aria-label={`View ${product.name}`}>
                  <div class="shop-media">
                    {product.featured ? <p class="shop-badge">Featured</p> : null}
                    {product.imageUrl ? <img src={product.imageUrl} alt={product.name} class="shop-image" loading="lazy" /> : <div class="shop-image shop-image--placeholder" aria-hidden="true"></div>}
                  </div>
                </a>

                <div class="shop-card-body">
                  <p class="shop-category">{CATEGORY_LABELS[product.category] ?? 'Styling'}</p>
                  <h3><a href={`/shop/${product.id}`}>{product.name}</a></h3>
                  <p class="shop-price">{formatGbp(product.pricePence)}</p>

                  <div class="shop-card-actions">
                    <button
                      type="button"
                      class="btn btn--primary"
                      data-add-to-cart
                      data-product-id={product.id}
                      data-product-name={product.name}
                      data-product-price-pence={String(product.pricePence)}
                      data-product-image-url={product.imageUrl ?? ''}
                    >
                      Add to cart
                    </button>
                    <a href={`/shop/${product.id}`} class="btn btn--secondary">Quick view</a>
                  </div>
                </div>
              </article>
            </li>
          ))}
        </ul>
      )}
    </section>


  </main>
</MainLayout>

<script>
  const root = document.querySelector('.shop-page');
  if (root) {
    const filterButtons = Array.from(root.querySelectorAll('[data-category-filter]'));
    const productItems = Array.from(root.querySelectorAll('[data-product-item]'));
    const productGrid = root.querySelector('.shop-grid');
    const promoSlot = root.querySelector('.shop-grid-promo-slot');
    const validCategories = new Set(filterButtons.map((button) => button.getAttribute('data-category-filter') || 'ALL'));
    const searchParams = new URLSearchParams(window.location.search);
    const initialCategoryParam = searchParams.get('category') || 'ALL';
    const initialCategory = validCategories.has(initialCategoryParam) ? initialCategoryParam : 'ALL';


    const applyCategoryFilter = (category) => {
      for (const item of productItems) {
        const productCategory = item.getAttribute('data-product-category') || 'STYLING';
        item.hidden = !(category === 'ALL' || productCategory === category);
      }

      if (productGrid && promoSlot) {
        const hasVisibleProducts = Array.from(productGrid.querySelectorAll('[data-product-item]')).some((item) => !item.hidden);
        promoSlot.hidden = !hasVisibleProducts;

      }

      for (const button of filterButtons) {
        const isActive = button.getAttribute('data-category-filter') === category;
        button.classList.toggle('is-active', isActive);
        button.setAttribute('aria-pressed', String(isActive));
      }
    };

    for (const button of filterButtons) {
      button.addEventListener('click', () => {
        const category = button.getAttribute('data-category-filter') || 'ALL';
        applyCategoryFilter(category);
        const nextUrl = new URL(window.location.href);
        if (category === 'ALL') {
          nextUrl.searchParams.delete('category');
        } else {
          nextUrl.searchParams.set('category', category);
        }
        window.history.replaceState({}, '', nextUrl);
      });
    }

    applyCategoryFilter(initialCategory);
  }
</script>