---
interface Contact16Props {
  className?: string;
}

const { className = "" } = Astro.props as Contact16Props;
---

<section id="book-demo" class={`contact16 ${className}`.trim()} aria-labelledby="contact16-title">
  <div class="container">
    <h2 id="contact16-title" class="contact16__title">Book a demo</h2>

    <div class="contact16__layout">
      <div class="contact16__info">
        <p class="contact16__description">
          Tell us what your barbershop needs and we’ll get back to you with the right website,
          booking and retail setup.

        </p>
      </div>

      <form class="contact16__form" action="#" method="post">
        <input type="hidden" name="intent" value="" data-demo-intent />
        <label>
          <span class="sr-only">Name</span>
          <input type="text" name="name" placeholder="Name*" required />
        </label>
        <label>
          <span class="sr-only">Email</span>
          <input type="email" name="email" placeholder="Email*" required />
        </label>
        <label>
          <span class="contact16__field-label">Tell us about your shop</span>
          <textarea
            name="message"
            placeholder="Tell us what your barbershop needs"
            rows="4"

            required
            data-demo-message
          ></textarea>

        </label>

        <button type="submit" class="btn btn--ghost contact16__submit">
          ↳ Book a demo
        </button>
      </form>
    </div>
  </div>
</section>
