---
import CartDrawer from './CartDrawer';

type Props = {
  testOrderMode?: boolean;
  publicDemoMode?: boolean;
};

const { testOrderMode = false, publicDemoMode = false } = Astro.props;
---

<CartDrawer client:load testOrderMode={testOrderMode} publicDemoMode={publicDemoMode} />
