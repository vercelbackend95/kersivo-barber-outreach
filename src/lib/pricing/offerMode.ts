/**
 * Public offer mode. When false, setup packages (Launch / Priority Growth)
 * stay in code for a later return but are hidden from landing + Launch Wizard.
 */
export const SHOW_SETUP_PLAN_CARDS = false;

/** Alias used by Launch Wizard / checkout paths. */
export const ENABLE_SETUP_FEES = SHOW_SETUP_PLAN_CARDS;
